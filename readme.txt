para que la base de datos  el login funcione
crear una base de datos llamada usuaros con los campos id, nombre y pass mediante el siguiente comando.


create table usuaros(
	id int NOT NULL AUTO_INCREMENT PRIMARY KEY,
	nombre varchar(100) NOT NULL,
	pass archar(200) NOT NULL
);


 AÑADIR UNOS REGSTROS !